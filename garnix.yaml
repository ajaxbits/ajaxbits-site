servers:
  - configuration: blogProd
    deployment:
      type: on-branch
      branch: main
  - configuration: blogProd
    deployment:
      type: on-pull-request
  - configuration: blogStaging
    deployment:
      type: on-branch
      branch: staging
